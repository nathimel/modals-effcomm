# Language measures

# complexity
comp: 
  lot_negation: True # whether to include negation in the LoT

# informativeness
inf:
  # Prior / communicative need distribution over meaning points
  prior: -8  
  # if int, represents a temperature parameter (exponentiated by 10) for an energy-based initialization. int or filename.
  # if string, specify the name of a file located at data/prior at the root of this repository.

  utility: half_credit # half_credit or indicator
  agent_type: literal # literal or pragmatic