# All modal universals, lexical and lexeme-level, will be measured.
lexeme_property: iff # what lexeme-level constraint to plot;  'iff' or 'sav' 
lexicon_property: null # what lexicon-level constraint to plot; 'dlsav' or 'deontic_priority', if null then don't.

counts: True # if True, then size will be mapped to counts
